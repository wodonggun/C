#include <stdio.h>
#include <string>

int main()
{

	string a;
	char b[10] = "best";

	const char *str = a.c_str();


	for (int i = 0; b[i] != '\0'; i++)
	{
		a += b[i];
	}


	printf("%s", a.c_str());

	return 0;

}
